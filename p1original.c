#include<stdio.h>
int main() {
  int a, b, area;
  printf("the base and height of the triangle are: \n ");
  scanf("%d %d", &a, &b);
  area =(a*b)/2;
  printf("the area of the triangle is %d : ", area);

  return area;
}

